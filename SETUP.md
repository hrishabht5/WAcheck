# CheckWA — Quick Start

```bash
# Terminal 1 — backend (port 3001)
cd backend && npm run dev

# Terminal 2 — frontend (port 3000)
cd frontend && npm run dev
```

Open **http://localhost:3000**.

For full documentation see:

- [`README.md`](README.md) — architecture, engines, CSV format, configuration
- [`docs/API.md`](docs/API.md) — HTTP endpoints and Socket.io event reference
- [`docs/RUNBOOK.md`](docs/RUNBOOK.md) — troubleshooting and deployment
- [`CHANGELOG.md`](CHANGELOG.md) — version history
